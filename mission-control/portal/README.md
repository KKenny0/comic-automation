# Mission Control Portal

Top-level navigation app for the Mission Control workspace.

## Setup

```bash
npm install
npm run dev
```

Open <http://localhost:3000>.

## Optional env

Create `.env.local` from `.env.local.example` if your app URLs differ.
